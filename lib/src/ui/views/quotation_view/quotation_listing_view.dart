import 'package:flutter/material.dart';

class QuotationListingView extends StatelessWidget {
  const QuotationListingView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
