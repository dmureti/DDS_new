enum Pages { home, routes, products, customers, adhoc }

enum PaymentModes { MPESA, Equitel, Cash, PayLater }

enum PasswordChangeType { reset, initial, user }

enum ConnectivityStatus { WiFi, Cellular, Offline }

enum CustomerType { Contract, Walk_In }

enum CrateType { Green, Orange }

enum CrateTxnType { Pickup, Return, Drop }

enum ServiceType { Background, Foreground, Persistent }
