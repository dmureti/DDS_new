import 'package:flutter/material.dart';

class ProductQuantityIconButton extends StatelessWidget {
  const ProductQuantityIconButton({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
