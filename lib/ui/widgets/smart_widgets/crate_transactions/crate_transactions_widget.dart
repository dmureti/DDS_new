import 'package:flutter/material.dart';

class CrateTransactionsWidget extends StatelessWidget {
  const CrateTransactionsWidget({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
